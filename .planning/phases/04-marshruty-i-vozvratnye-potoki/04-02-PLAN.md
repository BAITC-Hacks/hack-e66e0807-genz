---
phase: 04-marshruty-i-vozvratnye-potoki
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified: [solution/routes.py, solution/pipeline.py, frontend/src/components/Inspector.tsx, tests/test_routes.py, frontend/src/App.test.tsx, scripts/verify_delivery.py, docs/METHODOLOGY.md]
autonomous: true
requirements: [ROUTE-01, ROUTE-02, ROUTE-03]
estimate: {tokens: 19000, raw_tokens: 19000, tasks: 2, confidence: low}
must_haves:
  truths:
    - Cycles and routes are bounded, deduplicated, date-evidenced and navigable for arbitrary gids.
    - Analyst can tell repeated rows, ordered dates and unproven fund identity apart.
  artifacts: [tests/test_routes.py, scripts/verify_delivery.py, docs/METHODOLOGY.md]
  key_links: ["transactions.date → route ordered_date_examples → selected-node UI"]
---
<objective>Complete bounded routes/returns and independently verify evidence against input transactions.</objective>
<execution_context>@/home/nurik/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/04-marshruty-i-vozvratnye-potoki/04-PLANNED-CONTRACT.md @.planning/phases/04-marshruty-i-vozvratnye-potoki/04-01-SUMMARY.md @FINANCE-CASE/case.md</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: Add return cycles and observed date ordering</name><files>solution/routes.py, solution/pipeline.py, frontend/src/components/Inspector.tsx, tests/test_routes.py</files><behavior>Two- and three-node return cycles have unique canonical gid tuples; strict date examples are separated from same-day ambiguity; recurrence needs two distinct first-leg calendar days.</behavior><action>Per D-02/D-03/D-04, enumerate directed 2/3 cycles with numeric-gid rotational canonicalization and one result per topology. Scan numeric-gid-sorted candidate tuples with the 2000-per-node work budget before materializing at most 20 paths and 20 cycles. For each path/cycle scan at most 2000 sorted transaction-row combinations, deduplicate date tuples, save up to three examples per strict/ambiguous class, and count distinct first-leg days with strictly increasing dates. Set `repeated` only at two or more such days; flag date-search truncation so absence cannot be inferred. Count per-leg rows independently. Expose both date classes, recurrence and truncation in the selected-node UI without graph clutter; never claim amount/fund identity.</action><verify><automated>.venv/bin/python -m unittest tests.test_routes -v</automated></verify><done>Cycles, reciprocal edges, strict versus ambiguous dates, recurrence, work caps and caveats pass synthetic fixtures.</done></task>
<task type="auto" tdd="true"><name>Task 2: Verify real-data routes and case regression</name><files>scripts/verify_delivery.py, frontend/src/App.test.tsx, docs/METHODOLOGY.md</files><behavior>Independent validator resolves every route leg and example against actual edges/transactions; UI opens cited gids; all five case requirements still pass.</behavior><action>Add independent source-backed checks (D-01/D-05), including CSV headers and byte-stable baseline comparison, string gid references, no fabricated date pairs and absent optional section behavior. Update method text with topology/date distinction and selection limit. Run full batch time check under 300 seconds, all original must-haves, and cumulative optional mapping (boundary, temporal, card, missing-data requests, routes); record evidence in phase verification.</action><verify><automated>python3 run.py --check</automated></verify><done>Independent real-data verification passes under 300 seconds and the selected-node route is understandable on desktop/narrow screens.</done></task>
</tasks>
<threat_model><p>Untrusted dates and report text may mislead analysts (medium, mitigate): verify examples against source rows and display explicit limitations. Bounded enumeration mitigates resource exhaustion (medium). No package installs.</p></threat_model>
<verification>python3 run.py --check; npm --prefix frontend run test -- --run</verification>
<success_criteria>ROUTE-01–03 and existing five must-haves pass; route evidence never claims fund tracing.</success_criteria>
<output>Create 04-02-SUMMARY.md and 04-VERIFICATION.md when executed.</output>
