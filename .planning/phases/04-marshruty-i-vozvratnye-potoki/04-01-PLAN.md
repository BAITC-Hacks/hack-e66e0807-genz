---
phase: 04-marshruty-i-vozvratnye-potoki
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [docs/DATA-CONTRACT.md, solution/routes.py, solution/pipeline.py, frontend/src/contract.ts, frontend/src/components/Inspector.tsx, frontend/src/App.test.tsx, tests/test_routes.py]
autonomous: true
requirements: [ROUTE-01, ROUTE-02, ROUTE-03]
estimate: {tokens: 18000, raw_tokens: 18000, tasks: 2, confidence: low}
must_haves:
  truths:
    - A selected account with an observed A→B→C route shows two directed legs, their sums/counts and a statement that topology does not trace the same money.
    - The base report and three CSV exports retain their exact Phase 3 meaning.
  artifacts: [solution/routes.py, tests/test_routes.py, frontend/src/components/Inspector.tsx]
  key_links: ["solution.pipeline.run_pipeline → report.routes → frontend.parseReport → Inspector"]
---
<objective>Let the analyst inspect one real directed route end to end before expanding enumeration.</objective>
<execution_context>@/home/nurik/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@docs/DATA-CONTRACT.md @docs/ARCHITECTURE.md @.planning/phases/04-marshruty-i-vozvratnye-potoki/04-CONTEXT.md @.planning/phases/04-marshruty-i-vozvratnye-potoki/04-PLANNED-CONTRACT.md @.planning/phases/04-marshruty-i-vozvratnye-potoki/04-UI-SPEC.md</context>
<tasks>
<task type="tracer" tdd="true"><name>Task 1: Wire one directed route from validated data to selected-node evidence</name><files>solution/routes.py, solution/pipeline.py, frontend/src/contract.ts, frontend/src/components/Inspector.tsx, frontend/src/App.test.tsx</files><behavior>Fixture A→B→C yields two directed legs; selecting B shows gid links and leg amounts; an older report omitting routes still loads.</behavior><action>Use frozen Phase 4 field names in 04-PLANNED-CONTRACT.md (D-01). Start with a failing App behavioral assertion, then add deterministic bounded calculation over validated aggregate edges; include all nodes and preserve original records/CSV values (D-03/D-04). Wire `report.routes` through optional frontend parsing and selected-node evidence (D-05), labeling topological path separately from dated observations (D-02). Do not treat equal sums as fund identity.</action><verify><automated>npm --prefix frontend run test -- --run</automated></verify><done>A fixture route is visible in selected-node UI, older reports load, and pipeline run is ready for Task 2 independent source verification.</done></task>
<task type="auto" tdd="true"><name>Task 2: Cover route determinism and malformed optional evidence</name><files>docs/DATA-CONTRACT.md, tests/test_routes.py, frontend/src/contract.ts</files><behavior>Reversed input order yields identical route JSON; invalid gid/leg reference drops only the optional section and warns.</behavior><action>Copy the frozen Phase 4 schema to runtime contract per D-01. Expand fixtures for isolates, reciprocal links, self-loops, numeric 18-digit gid order, candidate-work cap/truncation and missing optional sections per D-03/D-04. Validate route gid references, leg orientation and finite amounts in parseReport while keeping mandatory node data available.</action><verify><automated>.venv/bin/python -m unittest tests.test_routes -v</automated></verify><done>Deterministic route fixtures and optional-data degradation pass.</done></task>
</tasks>
<threat_model><p>Untrusted parquet and report JSON cross into browser evidence. Mitigate tampered route references by server-side construction and optional frontend referential validation; bound enumeration to avoid denial of service. Severity: medium, disposition: mitigate. No package installs.</p></threat_model>
<verification>.venv/bin/python -m unittest tests.test_routes -v; npm --prefix frontend run typecheck</verification>
<success_criteria>A real path is inspectable with truthful limitations while required case outputs remain stable.</success_criteria>
<output>Create 04-01-SUMMARY.md when executed.</output>
