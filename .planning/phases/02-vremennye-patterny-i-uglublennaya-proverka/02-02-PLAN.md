---
phase: 02-vremennye-patterny-i-uglublennaya-proverka
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/contract.ts
  - frontend/src/App.tsx
  - frontend/src/App.test.tsx
  - frontend/src/index.css
autonomous: true
requirements: [TIME-01, EXPL-01]
estimate: {tokens: 23000, raw_tokens: 23000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "The selected node card shows dated temporal evidence, peak metrics, incoming profile, synchronous payers and next-data requests when supplied, while older version 1.0 reports still load (D-01, D-02, D-03, D-04, D-06, D-09)."
    - "Official shadcn Select controls filter role, cluster, seed and boundary in labeled exploration results; arbitrary gid search stays global (D-05, D-10)."
    - "A compact cluster overview shows counts, internal sum, hypothesis and member navigation, with reset and empty states (D-05, D-06)."
  artifacts: [frontend/src/contract.ts, frontend/src/App.tsx, frontend/src/App.test.tsx, frontend/src/index.css]
  key_links:
    - "parseReport accepts the optional node fields defined in docs/DATA-CONTRACT.md and validates their present values."
    - "Filters and cluster member buttons call the existing select(gid), opening the same graph and Inspector path."
---

<objective>
Give analysts a compact way to inspect temporal evidence, request missing data and narrow exploration by role and community.
Purpose: Extend the Phase 1 graph and card without disrupting global gid search or older reports.
Output: Local React UI changes and focused interaction tests.
</objective>

<execution_context>
@/home/nurik/.codex/gsd-core/workflows/execute-plan.md
@/home/nurik/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-vremennye-patterny-i-uglublennaya-proverka/02-CONTEXT.md
@.planning/phases/02-vremennye-patterny-i-uglublennaya-proverka/02-UI-SPEC.md
@.planning/phases/01-polnyy-lokalnyy-mvp/01-UI-SPEC.md
@docs/DATA-CONTRACT.md
@frontend/src/contract.ts
@frontend/src/App.tsx
@frontend/src/components/ui/select.tsx
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Show one optional temporal observation and next request in the selected node card</name>
  <files>frontend/src/contract.ts, frontend/src/App.tsx, frontend/src/App.test.tsx, frontend/src/index.css</files>
  <behavior>A report fixture with NodeRecord.temporal and next_data_requests renders dated one-day/two-day counts, peak count/share/baseline, incoming active days/total/median/distinct payer count, synchronous same-day payer evidence, and actionable request for a selected gid; a Phase 1 fixture without either optional field still parses and renders its original card.</behavior>
  <action>Per D-01 extend TypeScript types and parseReport handling for optional temporal and next_data_requests exactly as docs/DATA-CONTRACT.md defines; validate provided values, safely omit invalid optional evidence with the nonfatal message from 02-UI-SPEC, and retain all valid required Phase 1 node data. Absent optional values never display as measured zero. Per D-02, D-03 and D-09 render counts and dates as observed coincidences and peak activity, plus incoming profile and same-day distinct-payer concentration. Show explicit depth-four boundary caution: observed incoming activity does not prove a terminal recipient. Use the exact visible caution from 02-UI-SPEC that matching dates cannot establish identical funds. Per D-04 display each concrete request in backend order as plain text. Per D-06 use existing shadcn Card styling and compact card sections; preserve existing search and graph behavior. Write failing UI tests first, then implement and run them. Commit this completed task promptly; parent serializes push.</action>
  <verify><automated>npm --prefix frontend run test -- --run</automated></verify>
  <done>The selected node exposes the supplied evidence and requests, and older reports load without blank or broken sections.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Filter the exploration list and open cluster members</name>
  <files>frontend/src/App.tsx, frontend/src/App.test.tsx, frontend/src/index.css</files>
  <behavior>Combined role, cluster, seed and boundary controls narrow a labeled list of nodes; reset clears all controls; zero matches explains the empty state; a gid outside the filtered list remains findable by global search; cluster overview shows n_nodes, n_seed, sum_kzt_internal and hypothesis and opens a member in the same card/graph.</behavior>
  <action>Per D-05 and D-10 add compact labeled controls for role, cluster, seed and boundary using the installed official shadcn Select component for all four controls. Show visible result count, reset and the exact empty copy from 02-UI-SPEC. Build result rows from report.nodes, not only top_nodes, with deterministic order by descending priority then numeric gid; show 20 initially with explicit 20-row 'Показать ещё' steps. Preserve global arbitrary-gid search, selected node state and graph behavior even when the selection lies outside current filters. Add a compact scrollable cluster overview from report.clusters with the stated metrics; clicking 'Исследовать кластер {id}' sets the cluster filter and focuses the results heading without replacing the selected node. Existing member navigation continues to use select(gid). Per D-06 follow 02-UI-SPEC and the inherited shadcn design, responsive narrow layout, 44px controls, keyboard labels and count live region. Add interaction tests before implementation. Commit this completed task promptly; parent serializes push.</action>
  <verify><automated>npm --prefix frontend run test -- --run</automated></verify>
  <done>All filters compose and reset, cluster members open the existing inspector, old reports remain compatible and global search is unaffected.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Local report JSON to browser DOM | Report content and optional analyst text are untrusted input. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-04 | Tampering | parseReport | medium | mitigate | Validate optional counts, dates, finite ratios and request strings before rendering. |
| T-02-05 | Information disclosure | card rendering | low | mitigate | Render report strings as React text, never HTML injection. |
| T-02-06 | Denial of service | exploration filtering | low | mitigate | Filter in memory over the existing report node array; do not create new server routes or recursive graph traversal. |
</threat_model>

<verification>
Run the task test command and `npm --prefix frontend run build`. Wave 2 independently checks the built UI in a browser against actual data.
</verification>
<success_criteria>
The analyst can investigate temporal hypotheses and communities in the existing local interface while a Phase 1 report remains usable.
</success_criteria>
<output>
Create `.planning/phases/02-vremennye-patterny-i-uglublennaya-proverka/02-02-SUMMARY.md` when done.
</output>
