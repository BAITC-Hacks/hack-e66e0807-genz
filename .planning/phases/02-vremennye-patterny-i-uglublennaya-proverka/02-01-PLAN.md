---
phase: 02-vremennye-patterny-i-uglublennaya-proverka
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - solution/temporal.py
  - solution/pipeline.py
  - tests/test_temporal.py
autonomous: true
requirements: [TIME-01, EXPL-01]
estimate: {tokens: 18000, raw_tokens: 18000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "For each gid the report shows unique outgoing transactions following observed incoming transactions by one or two calendar days, with dated examples (D-02)."
    - "A reproducible, measured daily activity peak and incoming profile, including same-day distinct-payer concentration, are present when their thresholds hold (D-03, D-09)."
    - "Each node has a concrete next data request tied to its observed limitation; roles, priorities and the three CSV schemas remain identical (D-01, D-04)."
  artifacts: [solution/temporal.py, solution/pipeline.py, tests/test_temporal.py]
  key_links:
    - "run_pipeline passes validated transactions into temporal enrichment before serializing report.json."
    - "Optional NodeRecord.temporal and next_data_requests follow docs/DATA-CONTRACT.md exactly."
---

<objective>
Produce explainable temporal evidence and next-data requests in the existing local report.
Purpose: Let analysts test date-based hypotheses without implying transaction ordering within a day or tracing identical funds.
Output: Additive version 1.0 JSON fields; original CSVs, roles and priorities are preserved.
</objective>

<execution_context>
@/home/nurik/.codex/gsd-core/workflows/execute-plan.md
@/home/nurik/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-vremennye-patterny-i-uglublennaya-proverka/02-CONTEXT.md
@docs/DATA-CONTRACT.md
@solution/pipeline.py
@solution/analytics.py
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Carry one calendar-day observation and next request from parquet into report.json</name>
  <files>solution/temporal.py, solution/pipeline.py, tests/test_temporal.py</files>
  <behavior>Test a tiny valid parquet fixture with one incoming on July 1 and one outgoing on July 2 at the same gid: serialized node.temporal has one one-day match, one one-or-two-day match, the date pair, and a gid-specific next_data_requests entry. Existing schema_version, roles, priorities and exact CSV headers stay unchanged.</behavior>
  <action>Implement a pure temporal enrichment function over validated transactions, indexed by gid and calendar date, and call it from run_pipeline after analyze and before report serialization. Per D-01 keep schema_version '1.0' and CSV_SCHEMAS untouched; add only the documented optional node fields. Per D-02 count each qualifying outgoing transaction row once even if several incoming rows qualify; never use intraday order or compare amounts to assert fund identity. Per D-04 populate at least one concise actionable Russian next-data request naming the gid and its observed limitation. Start with the failing fixture test; run it red, implement, rerun green. Commit this completed task promptly; parent serializes push.</action>
  <verify><automated>.venv/bin/python -m unittest discover -s tests -p test_temporal.py -v</automated></verify>
  <done>The end-to-end fixture writes the documented fields and yields the expected one-day match and actionable request without altering required exports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete two-day, peak, and boundary semantics</name>
  <files>solution/temporal.py, tests/test_temporal.py</files>
  <behavior>Exactly two days counts only in the one-or-two-day bucket; same-day and later incoming dates do not count; several incoming candidates never multiply one outgoing; an isolate has zero counts and null peak; date pairs are sorted and capped at three; qualifying peak requires at least three incident rows and at least twice the full-period daily baseline, ties earliest date. Incoming profile counts observed days, amount, distinct payer gids and median incoming row amount; three distinct payers on one date produce synchronous_incoming, repeated rows from one payer do not raise distinct_payers; depth-four profile never creates a terminal assertion.</behavior>
  <action>Per D-02, D-03 and D-09 implement the complete docs/DATA-CONTRACT.md temporal definitions with deterministic iteration and finite output. Count a self-loop transaction once for incident daily activity. Compute incoming_profile and synchronous_incoming for all nodes including depth-four boundaries; qualify synchronous concentration only from at least three distinct payer gids on one calendar date, choose maximum distinct count then earliest date, and include every incoming row that date in tx_count and sum_kzt. Never infer terminal status from depth four. Per D-04 cover seed missing incoming history, depth-four output censoring, no observed incoming history, and temporal uncertainty with specific next-data requests, retaining a dataset/period coverage request if none of those branches applies. Add tests for all boundary cases, run red then green. Assert no temporal metric feeds role or priority. Commit this completed task promptly; parent serializes push.</action>
  <verify><automated>.venv/bin/python -m unittest discover -s tests -p test_temporal.py -v</automated></verify>
  <done>All calendar-window, peak, isolate and request rules pass tests; results are deterministic and remain explanatory hypotheses.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Organizer parquet to local report | Validated but untrusted dates and transaction rows become analyst evidence. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-01 | Tampering | temporal parsing | medium | mitigate | Reuse load_and_validate date parsing; tests reject missing/invalid dates and verify deterministic calendar arithmetic. |
| T-02-02 | Information disclosure | report explanations | low | mitigate | Generate requests only from observed gid, dates, counts and known dataset limits; do not invent account attributes. |
| T-02-03 | Denial of service | matching loop | medium | mitigate | Index incoming calendar dates by gid; do not compare every incoming to every outgoing globally. |
</threat_model>

<verification>
Run the task test command and existing `.venv/bin/python -m unittest discover -s tests -p test_analytics.py -v`. Check actual parquet output in wave 2.
</verification>
<success_criteria>
TIME-01 and the backend half of EXPL-01 are visible in report.json with no regression in version or CSV schemas.
</success_criteria>
<output>
Create `.planning/phases/02-vremennye-patterny-i-uglublennaya-proverka/02-01-SUMMARY.md` when done.
</output>
