---
phase: 05-ustoychivost-seti-pri-udalenii-uzlov
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified: [frontend/src/components/Inspector.tsx, frontend/src/App.test.tsx, scripts/verify_delivery.py, docs/METHODOLOGY.md]
autonomous: true
requirements: [RES-01, RES-02, RES-03]
estimate: {tokens: 12000, raw_tokens: 12000, tasks: 2, confidence: low}
must_haves:
  truths: ["Analyst can compare N values and understand that removal is hypothetical.", "Independent recomputation matches each scenario and baseline exports remain stable."]
  artifacts: [frontend/src/App.test.tsx, scripts/verify_delivery.py, docs/METHODOLOGY.md]
  key_links: ["independent graph recomputation → report.resilience → UI labels"]
---
<objective>Make top-N scenario comparison legible and independently verified.</objective>
<execution_context>@/home/nurik/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/05-ustoychivost-seti-pri-udalenii-uzlov/05-01-SUMMARY.md @.planning/phases/05-ustoychivost-seti-pri-udalenii-uzlov/05-UI-SPEC.md @FINANCE-CASE/case.md</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: Clarify scenario control and denominators</name><files>frontend/src/components/Inspector.tsx, frontend/src/App.test.tsx</files><behavior>Changing N updates removed gid list and both shares; absent section is unavailable rather than zero; keyboard controls work.</behavior><action>Per D-03, present bounded N selector, before/after metrics, explicit surviving/original denominators, clickable removed gids, and hypothetical label. Keep original graph view untouched. Cover N=0, N=max, absent and malformed optional data at desktop/narrow widths.</action><verify><automated>npm --prefix frontend run test -- --run</automated></verify><done>Scenario is understandable and accessible at supported widths.</done></task>
<task type="auto" tdd="true"><name>Task 2: Independently verify removal series and full case</name><files>scripts/verify_delivery.py, docs/METHODOLOGY.md</files><behavior>Independent source graph reproduces every published scenario; raw graph and three CSVs remain unchanged; batch stays under 300 seconds.</behavior><action>Implement a validator calculation from original parquet and existing priority list, including isolates and empty survivors (D-01/D-02). Document fixed order, denominator definitions, and hypothetical meaning. Run all five must-haves and cumulative six optional features; repair and rerun any failure.</action><verify><automated>python3 run.py --check</automated></verify><done>Independent real-data comparison and cumulative regression pass; phase verification records elapsed full run.</done></task>
</tasks>
<threat_model><p>Scenario results might be mistaken for real enforcement outcome (medium, mitigate explicit hypothetical wording and denominator display). Source/JSON references are validated before display (medium). No package installs.</p></threat_model>
<verification>python3 run.py --check; npm --prefix frontend run build</verification>
<success_criteria>RES-01–03 are auditable without modifying baseline analysis.</success_criteria>
<output>Create 05-02-SUMMARY.md and 05-VERIFICATION.md when executed.</output>
