# Phase 5 UI extension

Add a compact “Сценарий изъятия” section after priority evidence, with N selector 0..max and explicit fixed top-list order. Before/after table states removed gids, components, largest weak component, isolates, original-node count and surviving count. Label both shares with their denominators. The graph remains the original observed graph; scenario figures are not shown as actual observed removals. At narrow widths stack the comparison and allow gid navigation. Distinguish absent old-report data from N=0 baseline.
