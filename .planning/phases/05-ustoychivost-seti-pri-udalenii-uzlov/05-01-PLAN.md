---
phase: 05-ustoychivost-seti-pri-udalenii-uzlov
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [docs/DATA-CONTRACT.md, solution/resilience.py, solution/pipeline.py, frontend/src/contract.ts, frontend/src/components/Inspector.tsx, frontend/src/App.test.tsx, tests/test_resilience.py]
autonomous: true
requirements: [RES-01, RES-02, RES-03]
estimate: {tokens: 17000, raw_tokens: 17000, tasks: 2, confidence: low}
must_haves:
  truths: ["Selecting N=1 displays the original and surviving weak-component structure with both share denominators.", "All isolates and original exports remain intact."]
  artifacts: [solution/resilience.py, tests/test_resilience.py, frontend/src/components/Inspector.tsx]
  key_links: ["top_nodes fixed order → resilience scenarios → Inspector N selector"]
---
<objective>Show one honest fixed-priority removal scenario through calculation, report and UI.</objective>
<execution_context>@/home/nurik/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@docs/DATA-CONTRACT.md @.planning/phases/04-marshruty-i-vozvratnye-potoki/04-PLANNED-CONTRACT.md @.planning/phases/05-ustoychivost-seti-pri-udalenii-uzlov/05-CONTEXT.md @.planning/phases/05-ustoychivost-seti-pri-udalenii-uzlov/05-UI-SPEC.md</context>
<tasks>
<task type="tracer" tdd="true"><name>Task 1: Wire N=1 structural scenario end to end</name><files>solution/resilience.py, solution/pipeline.py, frontend/src/contract.ts, frontend/src/components/Inspector.tsx, frontend/src/App.test.tsx</files><behavior>Removing top priority node from a small graph changes weak-component count; selecting N=1 shows original and surviving denominators.</behavior><action>Use frozen Phase 5 schema per D-04. Start with a failing App behavioral assertion, then compute baseline and one induced-survivor graph using existing `top_nodes` order, copying rather than mutating original (D-01). Include isolates and both shares (D-02); parse optional section and show hypothetical comparison in selected-node workspace (D-03).</action><verify><automated>npm --prefix frontend run test -- --run</automated></verify><done>N=1 report fixture is selectable in UI; backend computation is independently checked in Task 2.</done></task>
<task type="auto" tdd="true"><name>Task 2: Add deterministic N=0..cap series</name><files>docs/DATA-CONTRACT.md, solution/resilience.py, tests/test_resilience.py, frontend/src/contract.ts</files><behavior>N=0 equals baseline; tie order follows numeric gid; all-node removal returns defined zero shares; old reports load.</behavior><action>Copy Phase 5 schema to runtime contract per D-04. Per D-01/D-02, generate every N from 0 to min(20, top list length), preserving fixed priority prefix. Calculate weak components, largest component, isolates and both denominators for each survivor graph, including empty and isolated cases. Validate exact removed prefix and safe numeric ranges in optional frontend parser.</action><verify><automated>.venv/bin/python -m unittest tests.test_resilience -v</automated></verify><done>Every scenario and edge case is deterministic, bounded and defined.</done></task>
</tasks>
<threat_model><p>Untrusted graph/report values cross into scenario display (medium, mitigate with validated bounds/prefix). Repeated graph copies can exhaust CPU (medium, mitigate with max N=20 and run-time measurement). No package installs.</p></threat_model>
<verification>.venv/bin/python -m unittest tests.test_resilience -v; npm --prefix frontend run typecheck</verification>
<success_criteria>RES-01/02/03 have a functioning N=1 slice and complete bounded scenario series.</success_criteria>
<output>Create 05-01-SUMMARY.md when executed.</output>
