---
phase: 07-ai-assistent-s-proveryaemymi-ssylkami
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified: [solution/assistant.py, solution/server.py, tests/test_assistant.py, docs/DATA-CONTRACT.md, docs/METHODOLOGY.md]
autonomous: true
requirements: [AI-01, AI-03, AI-04]
estimate: {tokens: 22000, raw_tokens: 22000, tasks: 2, confidence: low}
must_haves:
  truths: ["Questions about priorities, flows, clusters and optional evidence use only bounded report lookup tools.", "Every displayed factual citation resolves to an existing gid/edge/metric or answer states insufficient data."]
  artifacts: [solution/assistant.py, tests/test_assistant.py, docs/DATA-CONTRACT.md]
  key_links: ["allowlisted tool arguments → validated report object → citation resolver → answer"]
---
<objective>Expand read-only query coverage and enforce grounded, nonexecuting answers.</objective>
<execution_context>@/home/nurik/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/07-ai-assistent-s-proveryaemymi-ssylkami/07-AI-SPEC.md @.planning/phases/04-marshruty-i-vozvratnye-potoki/04-PLANNED-CONTRACT.md @.planning/phases/07-ai-assistent-s-proveryaemymi-ssylkami/07-01-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: Add bounded report-query tools</name><files>solution/assistant.py, tests/test_assistant.py, docs/DATA-CONTRACT.md</files><behavior>get_incoming/outgoing, five-source common-recipient lookup, priorities, cluster, route, resilience and anomaly queries return only matching existing records with bounded lengths and string gids; unknown gid fails explicitly.</behavior><action>Implement exact allowlist from 04-PLANNED-CONTRACT.md per D-03, including `get_common_recipients` over 2–20 existing source gids, with direct recipient counts, cited edges and deterministic count/sum/gid ranking, with typed parameter validation, finite limits, deterministic result ordering and no arbitrary SQL/code/file/network access. Resolve optional evidence only when present and valid. Freeze request/response/citation schema in runtime data contract; maintain baseline report compatibility. Test the canonical five-source question, all-five versus partial matches, no pair-date claim, hostile tool names, wrong types, huge limits and 18-digit ids.</action><verify><automated>.venv/bin/python -m unittest tests.test_assistant -v</automated></verify><done>Every allowed query is read-only, bounded and reference-safe.</done></task>
<task type="auto" tdd="true"><name>Task 2: Enforce citation and prompt-injection boundary</name><files>solution/assistant.py, solution/server.py, tests/test_assistant.py, docs/METHODOLOGY.md</files><behavior>Invented gid/edge and false numeric value under a real gid are rejected; route path/cycle indices and resilience scenario N resolve uniquely; injected report instruction cannot call extra tools.</behavior><action>Per D-04, accept only structured claim objects from model, resolve each exact reference/field/value in validated snapshot, including `route/path|cycle/index` discriminator, `resilience/N` exact scenario lookup and participant gid; for `get_common_recipients`, accept only model-selected recipient gid from bounded result and have server rederive count/sum from the complete matching directed edge set for the validated supplied sources, rendering the aggregate with one edge-sum citation per payer, then render factual sentences from server-owned templates. Never display arbitrary model factual prose. Invalid/missing claims return inability to answer. Treat all report text as untrusted quoted data, enforce 4 model/tool rounds and all numeric caps from 04-PLANNED-CONTRACT.md, and reject non-allowlisted functions. Document observation versus hypothesis, provider privacy boundary and limitations. Reject model-proposed aggregate count/sum, omitted matching edge and extra source edge. Test injection, unknown gid, false amount under real gid, ambiguous route index, wrong scenario N, boundary/seed and fake fund identity cases from AI-SPEC.</action><verify><automated>.venv/bin/python -m unittest tests.test_assistant -v</automated></verify><done>Adversarial fixtures cannot produce unsupported cited number or privileged tool action.</done></task>
</tasks>
<threat_model><p>Prompt injection and fabricated evidence (high, mitigate strict tool allowlist and server-side citation resolution). Exfiltration via configurable provider (high, mitigate disabled default, explicit env config and UI disclosure). No package installs.</p></threat_model>
<verification>.venv/bin/python -m unittest tests.test_assistant -v</verification>
<success_criteria>AI-03 and AI-04 grounding/security cases pass before UI expansion.</success_criteria>
<output>Create 07-02-SUMMARY.md when executed.</output>
