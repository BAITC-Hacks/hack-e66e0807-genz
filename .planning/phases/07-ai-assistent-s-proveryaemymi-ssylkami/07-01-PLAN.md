---
phase: 07-ai-assistent-s-proveryaemymi-ssylkami
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [docs/ARCHITECTURE.md, frontend/src/App.test.tsx, solution/assistant.py, solution/server.py, frontend/src/App.tsx, tests/test_assistant.py]
autonomous: true
requirements: [AI-01, AI-02, AI-03]
estimate: {tokens: 22000, raw_tokens: 22000, tasks: 2, confidence: low}
must_haves:
  truths: ["With a configured local CPU model, one natural-language question reaches the actual LLM and returns report-grounded gid evidence.", "With assistant disabled, the baseline UI, report and CSV exports work unchanged."]
  artifacts: [solution/assistant.py, tests/test_assistant.py, frontend/src/App.tsx]
  key_links: ["selected gid + question → loopback POST → bounded server query → configured LLM → validated citation → UI"]
---
<objective>Prove a genuine optional LLM answer through the local workbench and a validated report lookup.</objective>
<execution_context>@/home/nurik/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/07-ai-assistent-s-proveryaemymi-ssylkami/07-CONTEXT.md @.planning/phases/07-ai-assistent-s-proveryaemymi-ssylkami/07-AI-SPEC.md @.planning/phases/04-marshruty-i-vozvratnye-potoki/04-PLANNED-CONTRACT.md @solution/server.py @docs/ARCHITECTURE.md</context>
<tasks>
<task type="tracer" tdd="true"><name>Task 1: Ask a real configured model about one selected gid</name><files>solution/assistant.py, solution/server.py, frontend/src/App.tsx, frontend/src/App.test.tsx, tests/test_assistant.py</files><behavior>Configured mock protocol server records an actual LLM request with bounded get_node evidence; response cites an existing 18-digit gid; disabled mode reports unavailable while `/data/report.json` still works.</behavior><action>Per D-01/D-02, use Context7 resolve-library-id then query-docs for the chosen local inference provider and exact chat/tool protocol before implementation; use only Python standard library HTTP client unless verified need for a dependency, avoiding package installation. Document optional loopback POST and env-only configuration. Add a server-owned `get_node(gid)` over the validated report snapshot with an exact string-gid lookup, fixed question/body size and timeout (D-03). Call the configured LLM for a natural-language answer, validate the returned gid citation before displaying it, and present a closed-by-default selected-gid question entry in App (D-04/D-05). Start with failing API and App behavioral assertions. If provider unavailable, return explicit unavailable, preserving static routes (D-06).</action><verify><automated>.venv/bin/python -m unittest tests.test_assistant -v &amp;&amp; npm --prefix frontend run test -- --run</automated></verify><done>A configured LLM exchange and validated citation work end to end; disabled baseline works.</done></task>
<task type="auto" tdd="true"><name>Task 2: Bound transport and disabled-state behavior</name><files>docs/ARCHITECTURE.md, solution/assistant.py, solution/server.py, tests/test_assistant.py</files><behavior>Oversized question, invalid gid, timeout and malformed provider response produce bounded errors; no secret appears in response or logs.</behavior><action>Document the optional loopback request boundary in architecture. Per D-02/D-03/D-06, enforce loopback-only POST path, strict JSON body, size/time/call/output limits, env-only credential, no remote use unless explicitly configured, and safe provider errors. Reject path traversal, method misuse and arbitrary URL choice from user/model content. Keep static allowlist and three CSV downloads unaffected.</action><verify><automated>.venv/bin/python -m unittest tests.test_assistant tests.test_contracts -v</automated></verify><done>Transport caps, disabled mode and static-server regression pass.</done></task>
</tasks>
<threat_model><p>Untrusted question/provider output crosses local HTTP and model boundary (high, mitigate length/time caps, allowlisted route, citation validation). Secret disclosure (high, mitigate env-only key and redacted errors). Prompt injection (high, mitigate server-owned tools). No package installs planned; if execution adds one, obtain Package Legitimacy Audit before install.</p></threat_model>
<verification>.venv/bin/python -m unittest tests.test_assistant tests.test_contracts -v; npm --prefix frontend run typecheck</verification>
<success_criteria>AI-01 is a real configured LLM interaction, with safe disabled baseline.</success_criteria>
<output>Create 07-01-SUMMARY.md when executed.</output>
