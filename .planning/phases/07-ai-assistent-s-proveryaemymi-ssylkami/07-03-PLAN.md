---
phase: 07-ai-assistent-s-proveryaemymi-ssylkami
plan: 03
type: execute
wave: 3
depends_on: [07-02]
files_modified: [frontend/src/App.tsx, frontend/src/App.test.tsx, scripts/verify_delivery.py, scripts/verify_assistant_live.py, README.md, docs/DEMO.md]
autonomous: true
requirements: [AI-01, AI-02, AI-03, AI-04]
estimate: {tokens: 20000, raw_tokens: 20000, tasks: 3, confidence: low}
must_haves:
  truths: ["An analyst sees a concise answer with navigable citations and honest unavailable/unknown states.", "One configured local CPU model completes a live inference/tool/citation exchange; without it Phase 7 remains unverified.", "Without a model, one-command batch, exports and all earlier case features pass within 300 seconds."]
  artifacts: [frontend/src/App.test.tsx, scripts/verify_delivery.py, scripts/verify_assistant_live.py, README.md]
  key_links: ["validated assistant response → clickable gid → existing Inspector; disabled assistant → unchanged baseline journey"]
---
<objective>Finish optional assistant usability and independently prove no case regression.</objective>
<execution_context>@/home/nurik/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/07-ai-assistent-s-proveryaemymi-ssylkami/07-AI-SPEC.md @.planning/phases/07-ai-assistent-s-proveryaemymi-ssylkami/07-UI-SPEC.md @DESIGN.md @FINANCE-CASE/case.md</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: Present grounded answers without crowding analyst flow</name><files>frontend/src/App.tsx, frontend/src/App.test.tsx</files><behavior>Valid citation opens its gid; unanswered question says what data is missing; disabled, timeout, malformed answer and remote-provider disclosure are visible; keyboard/mobile interactions work.</behavior><action>Per D-05/D-06, finish optional collapsed panel with current gid context, input caps, loading, citations and observation/inference/unknown distinction. Render all text inertly, validate citation target before gid navigation, and retain Phase 3 priority→graph→evidence hierarchy and shadcn controls. Test narrow layout, focus, bad provider output and older reports.</action><verify><automated>npm --prefix frontend run test -- --run</automated></verify><done>Assistant interactions are accessible and do not obscure primary graph/card work.</done></task>
<task type="auto" tdd="true"><name>Task 2: Run required live local-model smoke and adversarial evaluation</name><files>scripts/verify_assistant_live.py, scripts/verify_delivery.py</files><precondition>A configured reachable local CPU inference endpoint and model name are available through the documented `ASSISTANT_*` environment variables; if absent, stop Phase 7 as unverified rather than treating a mock as live AI.</precondition><behavior>Five-source natural-language question triggers actual provider `get_common_recipients` call; answer cites shared gid, exact payer count and edge-sum; injected report text, fabricated number and unknown gid are refused.</behavior><action>Implement a CLI live smoke that fails unless it observes an actual configured provider response, the five-source `get_common_recipients` call and server-derived recipient/count/sum from the complete matching directed report-edge set and one exact edge-sum citation per supplied payer against the real report (D-01/D-04). Run the golden AI-SPEC set against controlled protocol mock separately via `tests.test_assistant`; wire its evaluation summary into `verify_delivery.py` so normal case verification includes mock grounding/security but never presents it as live acceptance. Capture sanitized pass/fail reasons and timings, with no question body, secret or answer logged by default.</action><verify><automated>.venv/bin/python scripts/verify_assistant_live.py --report output/report.json</automated></verify><done>Actual local CPU-model five-source inference and tool/citation/value validation pass; mock matrix reports groundedness, precision, caveats, injection, fallback and clarity separately.</done></task>
<task type="auto"><name>Task 3: Regress case and document opt-in operation</name><files>scripts/verify_delivery.py, README.md, docs/DEMO.md</files><action>Per D-02/D-06, document disabled default, local CPU model setup, env-only key policy, optional remote privacy notice and unavailable behavior; add a five-minute demo question with a verified gid citation while retaining the non-AI route. Run `python3 run.py --check` and the new AI evaluation plus reviewer journey for all five mandatory case capabilities and eight optional mappings: boundary, temporal, routes, anomalies, resilience, AI, generated node card, completeness requests. Verify original CSV schemas/values unchanged, full batch under 300 seconds, and repair/retest failures.</action><verify><automated>python3 run.py --check</automated></verify><done>Offline baseline, cumulative judge journey and documentation pass; live gate from Task 2 is recorded separately; no credentials committed.</done></task>
</tasks>
<threat_model><p>Model answer can mislead users (high, mitigate server validation and visible citation/limitations). Remote data disclosure (high, mitigate opt-in configuration and clear UI notice). HTML injection (medium, render as text). No package installs planned.</p></threat_model>
<verification>.venv/bin/python -m unittest tests.test_assistant -v; .venv/bin/python scripts/verify_assistant_live.py --report output/report.json; python3 run.py --check; npm --prefix frontend run test -- --run; npm --prefix frontend run build</verification>
<success_criteria>AI-01–04 and all case criteria are evidenced, including the disabled offline path.</success_criteria>
<output>Create 07-03-SUMMARY.md and 07-VERIFICATION.md when executed.</output>
