---
phase: 01-polnyy-lokalnyy-mvp
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified: [frontend/package.json, frontend/index.html, frontend/vite.config.ts, frontend/tsconfig.json, frontend/src/main.tsx, frontend/src/App.tsx, frontend/src/index.css, frontend/src/components/ui/button.tsx, frontend/src/components/ui/card.tsx, frontend/src/components/Graph.tsx, frontend/src/contract.ts, frontend/src/App.test.tsx, frontend/scripts/ui-smoke.mjs]
autonomous: true
requirements: [DATA-03, ROLE-02, ROLE-03, UI-01, UI-02, UI-03, UI-04, TEST-01]
estimate:
  tokens: 23000
  raw_tokens: 23000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - An analyst searches any string gid and sees its role, numerical evidence, warnings and directed neighbors.
    - Ranked rows and graph selection share the same selected node; isolates remain reachable.
    - Roles and communities have visible legends and all CSVs can be downloaded.
    - The Russian local UI handles loading, failure, incompatible schema, no result and narrow screens.
  artifacts:
    - path: frontend/src/App.tsx
      provides: connected dashboard, search, rankings, downloads and states
    - path: frontend/src/components/Graph.tsx
      provides: directed neighborhood with role/cluster legend
    - path: frontend/src/App.test.tsx
      provides: contract, search, selection and state tests
  key_links:
    - from: frontend/src/App.tsx
      to: /data/report.json
      via: fetch and schema-version validation
    - from: frontend/src/App.tsx
      to: /data/nodes_roles.csv
      via: three explicit download links
    - from: frontend/src/components/Graph.tsx
      to: frontend/src/App.tsx
      via: shared string selectedGid state
---

<objective>
Let an analyst answer who to inspect and why through a clean Russian React/Vite shadcn interface. Per D-01/D-02/D-05 frontend owns frontend/ exclusively and starts against an explicitly synthetic test fixture matching the established contract. It does not wait for analytics output.
</objective>
<execution_context>
@/home/nurik/.codex/gsd-core/workflows/execute-plan.md
@/home/nurik/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-polnyy-lokalnyy-mvp/01-CONTEXT.md
@.planning/UI-SPEC.md
@.planning/phases/01-polnyy-lokalnyy-mvp/01-UI-SPEC.md
@docs/DATA-CONTRACT.md
</context>

<tasks>
<!-- Notify the parent after every small completed file batch and each passing task for immediate serialized commits; workers never run git. The parent has verified official shadcn MCP/registry access; use /tmp/shadcn_mcp_probe.py if direct MCP exposure is unavailable. Frontend dependency installation is owned by the UI executor only after reading the parent's package provenance/audit note. -->
<task type="tracer" tdd="true">
  <name>Task 1: Prove report-to-search-to-card using the local app</name>
  <files>frontend/package.json, frontend/index.html, frontend/src/main.tsx, frontend/src/App.tsx, frontend/src/App.test.tsx</files>
  <precondition>The orchestrator has supplied audited frontend dependencies and the shared contract exists; read UI-SPEC.md once available before visual implementation.</precondition>
  <behavior>A mocked fetch returns a synthetic contract Report with a gid larger than JavaScript safe integer; entering that exact string selects the matching node and displays its evidence.</behavior>
  <action>Use React/Vite and actual shadcn components per D-01. Create the frontend app with scripts dev, build, typecheck and test; wire fetch('/data/report.json') into one stateful root and gid search into displayed card. Define exact string identifiers and Report shape from DATA-CONTRACT without numeric coercion. Write the test first with explicitly synthetic fixture embedded in the test, never in production fallback. Preserve scaffold supplied by the orchestrator; package installation/legitimacy is orchestrator-owned. Have the first working path show one fetched node and evidence, with real fetch-error handling. No runtime CDN assets. Existing contract constitutes completed architecture discovery; check library syntax via Context7 before unfamiliar library code.</action>
  <verify><automated>npm --prefix frontend run test -- --run</automated></verify>
  <done>A mounted app can fetch a contract fixture, find an exact gid, and show its evidence; the same production fetch path accepts generated report.json.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Explore directed neighbors, rankings and explained roles</name>
  <read_first>Read the canonical UI-SPEC interaction contract: implement pan, zoom in/out, reset-to-fit, role/cluster fill switch, initial top-node selection, visible rendered counts, complete selectable neighbor lists, cluster summary, exact Russian state copy, and unknown-gid preservation of the existing selection.</read_first>
  <files>frontend/src/App.tsx, frontend/src/components/Graph.tsx, frontend/src/contract.ts, frontend/src/App.test.tsx</files>
  <behavior>Clicking a ranked node and searching it produce the same selection; an isolated node remains visible; incoming and outgoing links are distinguishable; seed/depth warnings and role metrics appear.</behavior>
  <action>Expand the selected node into a directed interactive neighborhood with visible arrowheads, labeled amount/count details and role/cluster encodings plus legend. Include full-dataset role/cluster context and allow any gid regardless of filtering. Use deterministic local SVG layout and bounded neighborhood rendering sufficient for all immediate links; disclose any view limit and provide access to omitted neighbors. Connect top-list rows and graph-node buttons to one selectedGid state. Show role, heuristic role_score and priority_score, community, evidence, degrees, in/out amounts, seed ancestors, depth and warnings from Report. Explain null pass-through as unavailable and depth=4 as censored per D-03. Render isolated selected nodes with explicit no-observed-links state. Add role and cluster selection where required by UI-SPEC. Provide exact links for all three CSVs. Prevent synthetic fixture data from entering production views.</action>
  <verify><automated>npm --prefix frontend run test -- --run</automated></verify>
  <done>All gid are searchable and reachable from graph/top list, with directions, legends, numerical explanations and honest limitations.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Finish accessible shadcn presentation and browser acceptance path</name>
  <files>frontend/src/index.css, frontend/src/components/ui/button.tsx, frontend/src/components/ui/card.tsx, frontend/src/App.test.tsx, frontend/scripts/ui-smoke.mjs</files>
  <behavior>Loading, fetch error, unsupported schema, empty dataset and unknown gid produce distinct actionable Russian states; keyboard selection works; the browser smoke path searches and follows a ranked node.</behavior>
  <action>Apply UI-SPEC light tokens, restrained accent, readable Russian labels, numbers and typographic hierarchy with actual shadcn components. Supply visible focus, semantic buttons/input labels, accessible statuses and sufficient contrast; convey roles beyond color. Make desktop graph/detail layout stack on narrow screens without page overflow. Finish state handling in root through existing component props; if App.tsx needs changes retain ownership and record it in summary. Add Playwright smoke script using the orchestrator-provisioned browser runner against a supplied base URL; assert loaded dataset, exact gid search, rank selection, visible directed graph, isolate/boundary details, all download links, keyboard and 390px layout. Keep test setup inline in App.test.tsx; do not create a separate test-setup module. Keep the inspector as a local component in App.tsx rather than a separate NodeDetails module. Production build must contain local bundled assets only. Record shadcn MCP availability honestly and use official component sources if unavailable.</action>
  <verify><automated>npm --prefix frontend run test -- --run &amp;&amp; npm --prefix frontend run build</automated></verify>
  <done>Component behavior tests and build pass; smoke script is ready for real-output browser verification in 01-03; all required UI states and keyboard/narrow-screen behavior exist.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| report.json → DOM | File strings and malformed records must be handled safely. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-U1 | Tampering | contract.ts/App.tsx | medium | mitigate | Validate schema version, array shapes and identifier/number types before rendering. |
| T-01-U2 | Elevation of privilege | evidence and labels | medium | mitigate | Render strings as React text; no HTML insertion from report fields. |
| T-01-U3 | Denial of service | Graph.tsx | medium | mitigate | Bound rendered neighborhood while keeping all gid searchable and neighbor access explicit. |
| T-01-SC | Tampering | frontend dependencies | high | transfer | Orchestrator owns package audit and provisioning; no dependency-install task here. |
</threat_model>
<verification>Run tests/build; integration serves real outputs and invokes frontend/scripts/ui-smoke.mjs. Human-check: read layout at desktop/390px and inspect arrow direction, focus and caveat clarity.</verification>
<success_criteria>UI-01/02/03/04 and browser aspects of DATA-03, ROLE-02/03 and TEST-01 are observable in the production build.</success_criteria>
<output>Create .planning/phases/01-polnyy-lokalnyy-mvp/01-02-SUMMARY.md with commands, tested fixture separation, UI caveats and shadcn tool availability.</output>
