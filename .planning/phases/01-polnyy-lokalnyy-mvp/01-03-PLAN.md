---
phase: 01-polnyy-lokalnyy-mvp
plan: "03"
type: execute
wave: 2
depends_on: [01-01, 01-02]
files_modified: [solution/__main__.py, solution/server.py, tests/test_contracts.py, scripts/verify_delivery.py, README.md, docs/DEMO.md, docs/ARCHITECTURE.md]
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03, ROLE-01, ROLE-02, ROLE-03, CLUS-01, RANK-01, UI-01, UI-02, UI-03, UI-04, SHIP-01, SHIP-02, TEST-01]
estimate:
  tokens: 14000
  raw_tokens: 14000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - One documented command creates all CSVs and JSON from real parquet in less than 300 seconds.
    - A local server serves the built UI and generated data/downloads without CDN or API infrastructure.
    - Actual export invariants, reproducibility and the browser's principal workflow pass.
    - README, architecture diagram and five-minute demo allow another person to reproduce and explain results.
  artifacts:
    - path: solution/__main__.py
      provides: one-command batch entry
    - path: solution/server.py
      provides: loopback static UI/data serving
    - path: tests/test_contracts.py
      provides: independent CSV/JSON/determinism validation
    - path: scripts/verify_delivery.py
      provides: measured real-data and static-serving acceptance report
    - path: README.md
      provides: installation, exact commands, formulas, caveats, scale discussion
    - path: docs/DEMO.md
      provides: five-minute live-run script and 2–3 data-selected nodes
  key_links:
    - from: solution/__main__.py
      to: solution/pipeline.py
      via: run_pipeline(data_dir, out_dir)
    - from: solution/server.py
      to: frontend/dist and output
      via: root static app plus /data mapping
    - from: scripts/verify_delivery.py
      to: frontend/scripts/ui-smoke.mjs
      via: browser verification on real served output
---

<objective>
Deliver the reproducible submission and prove every requirement on actual inputs, per D-04/D-06. Consume parallel analytics/UI outputs through the existing contract. Shared CLI and dependency preparation may be performed by the orchestrator while wave 1 runs; this plan performs final integration and verification once both outputs exist.
</objective>
<execution_context>
@/home/nurik/.codex/gsd-core/workflows/execute-plan.md
@/home/nurik/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-polnyy-lokalnyy-mvp/01-CONTEXT.md
@docs/DATA-CONTRACT.md
@.planning/phases/01-polnyy-lokalnyy-mvp/01-01-SUMMARY.md
@.planning/phases/01-polnyy-lokalnyy-mvp/01-02-SUMMARY.md
@FINANCE-CASE/case.md
</context>

<tasks>
<!-- Execute Python commands with .venv/bin/python. Notify the parent after each small completed file batch and passing task for immediate serialized commits; workers never run git. Observed real input: 2248 nodes, 3119 edges, 4840 transactions, 81 seeds, 19 isolates, 35 weak components and 365890012.01 KZT. Report these measured facts and the case-document component-count discrepancy; never force calculations to match the prose. Compare currency sums with explicit 0.01 KZT tolerance. -->
<task type="tracer" tdd="true">
  <name>Task 1: Run actual parquet through the CLI and inspect one node in the served UI</name>
  <files>solution/__main__.py, solution/server.py, tests/test_contracts.py</files>
  <behavior>CLI forwards user data/out paths; server returns the built page, exact report.json and each CSV; invalid/missing files fail clearly and path traversal is rejected.</behavior>
  <action>Per D-01/D-05 create or finish python -m solution --data FINANCE-CASE/data --out output around the agreed run_pipeline. Implement python -m solution.server --data output --ui frontend/dist --port 8000 using loopback by default and separate allowlisted static roots. Serve /data/report.json and three named CSVs directly; no copying synthetic fixtures. Prevent paths escaping roots. Make one real search/card path work against generated output and actual bundled UI. Preserve the frontend/ and analytics owners' files; request narrow owner fixes for interface bugs.</action>
  <verify><automated>.venv/bin/python -m unittest discover -s tests -p test_contracts.py -v</automated></verify>
  <done>CLI calculation and local static serving have automated checks; actual browser page consumes real report and downloads.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Independently verify all submission invariants and measured real execution</name>
  <files>tests/test_contracts.py, scripts/verify_delivery.py</files>
  <behavior>Validator rejects missing/duplicate gid, bad schemas, unbounded scores, long/empty evidence, broken cluster/rank references, non-string JSON IDs and invalid numeric JSON; repeated CSV output is byte-identical.</behavior>
  <action>Per D-04 run real inputs twice in separate temporary outputs, measure elapsed wall time for each full batch and assert each is under 300 seconds. Validate exact CSV column order; one role row per nodes.parquet gid; six-role dictionary; scores [0,1]; 1–200 evidence characters; every cluster membership and aggregated n_nodes/n_seed/internal sum; top ≥20 unique consecutive ranks with ordered scores; report version, string IDs including edge endpoints/top_gids, finite fields, and input transaction sums/counts. Compare deterministic CSV bytes while excluding elapsed_seconds from JSON repeat checks. Explicitly test seed/isolate/depth=4 exclusions. Build UI, start server on loopback, invoke the frontend smoke script against real output and fetch each CSV to verify content. Emit a concise report with measured counts, timings, commands and pass/fail. Fix failing criteria through owning agents and re-run relevant checks; do not mark complete from file existence.</action>
  <verify><automated>.venv/bin/python scripts/verify_delivery.py --data FINANCE-CASE/data --out output</automated></verify>
  <done>All required checks pass on real data with two measured runs below 300 seconds, identical CSVs and successful browser workflow; failures block completion.</done>
</task>
<task type="auto">
  <name>Task 3: Publish reproducible instructions, architecture and a live demonstration</name>
  <files>README.md, docs/DEMO.md, docs/ARCHITECTURE.md</files>
  <action>Per D-04/D-06 document exact tested environment and dependency installation, one batch command, build/serve commands, output schemas and actual observed runtime/counts. Include every implemented role rule, threshold, precedence, heuristic score/ranking formula and community method from the analytics owner. Explain no ground truth, depth-4 censoring, incomplete seed/incoming flow, intra-bank/July/5000-KZT limitations, isolates and lack of personal attributes. Describe a credible ~1M-node scaling path: columnar/chunked aggregation, sparse/compiled graph operations, sampled centrality and neighborhood serving; implementation is outside this phase. Add Mermaid data → validation → metrics → roles/community/ranking → CSV/JSON → UI diagram. Write a five-minute script with live pipeline and 2–3 dynamically selected actual nodes including a boundary or isolated example; give real explanations from output without changing classification using example gid. Check every README command against the built result, record tests and honest limitations. Improvements begin only after green verifier per D-06.</action>
  <verify><automated>.venv/bin/python -m solution --help &amp;&amp; .venv/bin/python -m solution.server --help &amp;&amp; .venv/bin/python -m unittest discover -s tests -v</automated></verify>
  <done>A new user can install, calculate, serve, explain arbitrary gid and present the five-minute demonstration using the documented artifacts.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| HTTP request → filesystem | Local server must not expose arbitrary files. |
| source data → published exports | Submission must preserve data semantics and avoid silent corruption. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-I1 | Information disclosure | solution/server.py | high | mitigate | Loopback default, allowlisted data names, root containment and traversal tests. |
| T-01-I2 | Tampering | CSV/JSON outputs | medium | mitigate | Independent exact schema/reference/numerical validation and deterministic reruns. |
| T-01-I3 | Repudiation | delivery documentation | medium | mitigate | Record real inputs, measured runtime, commands, rules and verified counts. |
| T-01-SC | Tampering | dependency installation | high | transfer | Orchestrator provides official package audit and pinned reproducible dependencies; no install task here. |
</threat_model>
<verification>Run scripts/verify_delivery.py and all unittests, npm build and real browser smoke. End-of-phase human-check: five-minute demo and narrow-screen keyboard use; do not substitute human review for automated acceptance.</verification>
<success_criteria>All 15 phase requirements have passing evidence; no unresolved high-severity security issue; no improvements are substituted for mandatory delivery.</success_criteria>
<output>Create .planning/phases/01-polnyy-lokalnyy-mvp/01-03-SUMMARY.md with measured timing, observed counts, exact commands, browser evidence and remaining limitations.</output>
