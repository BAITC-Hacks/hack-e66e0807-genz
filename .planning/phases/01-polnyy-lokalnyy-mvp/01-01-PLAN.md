---
phase: 01-polnyy-lokalnyy-mvp
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [solution/analytics.py, solution/pipeline.py, tests/test_analytics.py]
autonomous: true
requirements: [DATA-02, DATA-03, ROLE-01, ROLE-02, ROLE-03, CLUS-01, RANK-01, TEST-01]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - Every input gid survives exactly once, including isolated seed nodes.
    - Every node has an explained role, bounded scores, and a deterministic community.
    - Boundary censoring and seed incompleteness affect rules and remain visible.
    - Real parquet inputs produce all four contract outputs with at least 20 ranked unique nodes.
  artifacts:
    - path: solution/pipeline.py
      provides: run_pipeline(data_dir, out_dir) returning Report and writing CSV/JSON
    - path: solution/analytics.py
      provides: deterministic features, rule precedence, communities, ranking
    - path: tests/test_analytics.py
      provides: role, boundary, seed, isolate, and real-pipeline regression checks
  key_links:
    - from: solution/pipeline.py
      to: FINANCE-CASE/data
      via: nodes, edges, transactions parquet reads
    - from: solution/analytics.py
      to: docs/DATA-CONTRACT.md
      via: exact roles, fields, gid encoding, and output schemas
---

<objective>
Compute trustworthy, explainable hypotheses and submission outputs from real inputs. Per D-01, D-02, D-03 and D-05, own only analytics files; the existing DATA-CONTRACT is the completed interface prerequisite. Analytics and UI execute concurrently. Output: deterministic Python batch implementation and meaningful tests.
</objective>
<execution_context>
@/home/nurik/.codex/gsd-core/workflows/execute-plan.md
@/home/nurik/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-polnyy-lokalnyy-mvp/01-CONTEXT.md
@docs/DATA-CONTRACT.md
@FINANCE-CASE/case.md
@FINANCE-CASE/additional.md
@FINANCE-CASE/starter/starter.py
</context>

<tasks>
<!-- Execute Python commands with .venv/bin/python (the orchestrator-provisioned environment). Notify the parent after every small completed file batch and each passing task so it can commit immediately; workers never run git. Compare monetary aggregates with an explicit tolerance (at most 0.01 KZT) because real floating-point error is 5.8e-11; counts remain exact. -->
<task type="tracer" tdd="true">
  <name>Task 1: Read real parquet and produce an explained node through the full file pipeline</name>
  <files>solution/pipeline.py, solution/analytics.py, tests/test_analytics.py</files>
  <precondition>docs/DATA-CONTRACT.md exists and the orchestrator has provisioned audited pandas, parquet engine and networkx dependencies.</precondition>
  <behavior>A small parquet fixture traverses run_pipeline and produces a nonempty role/evidence and exact output headers; all nodes including an isolate survive; malformed references and inconsistent pair sums/counts fail clearly.</behavior>
  <action>Start with a failing unittest pipeline test. Implement run_pipeline(data_dir: Path, out_dir: Path) returning Report and writing report.json plus the three CSV files per D-01/D-02. Read all three inputs, validate required columns, unique nodes/pairs, references, finite nonnegative amounts, and transaction aggregation against both edge sum_kzt and n_tx. Add nodes before edges. Implement one real peripheral classification path with numeric evidence, community membership, and bounded scores; subsequent tasks expand this same path. Serialize all JSON identifiers as strings, CSV identifiers as integers, null instead of undefined ratios, UTF-8 and finite JSON. Preserve source materials. Expose elapsed_seconds and actual observed data counts. Keep root dependency files and CLI in integration ownership per D-05.</action>
  <verify><automated>.venv/bin/python -m unittest discover -s tests -p test_analytics.py -v</automated></verify>
  <done>A test-created parquet dataset completes real file read → metrics → classification → contract writes, including isolate retention; invalid input produces useful errors.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Expand to all six explained roles with censoring-aware rules</name>
  <files>solution/analytics.py, solution/pipeline.py, tests/test_analytics.py</files>
  <behavior>Fixtures independently activate each role; overlapping rules follow documented precedence; depth=4 zero-outflow and seed zero-outflow cannot become terminal from that fact; scores are finite in [0,1], evidence is 1–200 characters.</behavior>
  <action>Per D-03 implement in/out degree, sums, pass-through, seed reachability and bounded structural centrality. Define named thresholds and precedence for coordinator, consolidator, distributor, transit, terminal and peripheral; allow analytics discretion to choose defensible thresholds, then expose exact rules and numbers for README/card explanations. Terminal requires observed incoming flow, retained-flow evidence, non-seed status and non-censored depth; do not treat an observed balance as full account balance. pass_through is null for seed or zero incoming. Flag boundary_censored, seed and incomplete observed inflows in warnings. Derive role_score as heuristic rule strength and evidence from metrics, never from gid or asserted guilt. Test all rule boundaries and ties. Supply rule definitions to integration owner without editing README.</action>
  <verify><automated>.venv/bin/python -m unittest discover -s tests -p test_analytics.py -v</automated></verify>
  <done>Every role has a formal tested rule and priority; arbitrary nodes have numerical explanations and applicable caveats.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Deliver deterministic communities and ranked priorities on all real inputs</name>
  <files>solution/analytics.py, solution/pipeline.py, tests/test_analytics.py</files>
  <behavior>Every node belongs to one community including isolated nodes; internal community sums match edges; top list is unique, descending, stable on ties; two runs produce identical CSV bytes.</behavior>
  <action>Compute weighted communities on a documented undirected projection while preserving directed metrics, fixed random seed and stable cluster numbering by numeric gid. Preserve isolated singleton communities. Aggregate n_nodes, n_seed, internal sum, top_gids and cautious numerical hypotheses. Rank using documented normalized structural/flow signals, not seed guilt; emit at least 20 nodes when dataset size permits, descending priority_score with numeric gid tie-breaks and explanatory why. Complete all Report fields and exact CSV schemas including semicolon-separated top_gids. Run on FINANCE-CASE/data, compare observed counts with announced counts as diagnostics, never hardcode outputs. Keep centrality computation bounded to meet the 300-second budget. Send rule formulas and measured output location to the integration owner.</action>
  <verify><automated>.venv/bin/python -m unittest discover -s tests -p test_analytics.py -v</automated></verify>
  <done>Real data yields complete schemas, all input gid, communities and ≥20 ranked unique nodes; deterministic tests and all analytical edge cases pass.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| parquet → analytics | Files may contain inconsistent references, schema or numerical values. |
| analytics → published artifacts | Invalid numbers or misleading claims would misdirect analysis. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-A1 | Tampering | pipeline input validation | medium | mitigate | Validate schemas, references, per-pair sums/counts before output. |
| T-01-A2 | Denial of service | centrality/community analysis | medium | mitigate | Bounded centrality and measured real batch runtime. |
| T-01-A3 | Repudiation | explanations | medium | mitigate | Deterministic rule metadata, numeric evidence, warnings and heuristic language. |
| T-01-SC | Tampering | runtime dependencies | high | transfer | Orchestrator owns package legitimacy audit and dependency provisioning before this plan. |
</threat_model>
<verification>Run analytics unittests and real pipeline. Integration plan independently validates schemas and repeated output equality.</verification>
<success_criteria>DATA-02/03, ROLE-01/02/03, CLUS-01, RANK-01 covered by passing behavior tests and real exported artifacts.</success_criteria>
<output>Create .planning/phases/01-polnyy-lokalnyy-mvp/01-01-SUMMARY.md with rule formulas, counts, runtime, test results and integration notes.</output>
