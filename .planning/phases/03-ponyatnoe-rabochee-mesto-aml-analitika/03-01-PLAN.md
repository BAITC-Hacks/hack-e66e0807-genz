---
phase: 03-ponyatnoe-rabochee-mesto-aml-analitika
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [DESIGN.md, frontend/src/App.tsx, frontend/src/components/Graph.tsx, frontend/src/components/Inspector.tsx, frontend/src/index.css, frontend/src/App.test.tsx, frontend/src/components/Graph.test.tsx]
autonomous: true
requirements: [UX-01, UX-02, UX-03, UI-04]
must_haves:
  truths:
    - The first screen presents priorities, a large directional graph, and the selected account inspector together.
    - Incoming and outgoing lanes show arrows, amount labels, full selected gid, role or cluster colors, and explicit pagination for high-degree nodes.
    - Global gid search, combined filters, cluster exploration, twenty priority rationales, temporal evidence, warnings and three CSV exports remain available.
  artifacts: [frontend/src/App.tsx, frontend/src/components/Graph.tsx, frontend/src/index.css, frontend/src/App.test.tsx, frontend/src/components/Graph.test.tsx]
  key_links:
    - Every navigation entry calls the same selected-gid state and reads the unchanged report contract.
    - Graph lanes use directed report edges and do not infer missing transactions or terminal status.
---

<objective>Replace the vertically stacked, unreadable graph UI with a minimal analyst workbench. Preserve the local data contract and all existing analyst capabilities.</objective>
<context>
@AGENTS.md
@docs/DATA-CONTRACT.md
@FINANCE-CASE/case.md
@.planning/phases/03-ponyatnoe-rabochee-mesto-aml-analitika/03-UI-SPEC.md
</context>
<tasks>
<task type="auto">
<name>Task 1: Build the directional graph and analyst workbench</name>
<files>frontend/src/App.tsx, frontend/src/components/Graph.tsx, frontend/src/index.css</files>
<action>Use installed official shadcn controls. Build three-column desktop workspace, priorities and filterable all-node list at left, directional graph center, compact inspector right. Replace radial layout with deterministic incoming/selected/outgoing lanes, labeled amounts, arrowheads, full gids on focus and selected node, role/cluster modes, paging and zoom controls. Keep isolate/boundary/seed cautions, global gid search, cluster exploration, accessible progressive temporal/details/methodology sections, exports and robust loading/error states. No dependency or backend changes required.</action>
<verify><automated>npm --prefix frontend run typecheck</automated></verify>
<done>All required interactions are implemented and TypeScript passes; exact ready files are sent to parent for serialized commit.</done>
</task>
<task type="auto">
<name>Task 2: Verify redesigned interactions and responsive build</name>
<files>frontend/src/App.test.tsx, frontend/src/components/Graph.test.tsx</files>
<action>Add semantic tests for graph lanes/direction/pagination, neighbor selection, isolate and boundary context. Adapt existing interaction tests to progressive disclosure while preserving their assertions. Run all tests and production build; parent independently inspects actual-data desktop/mobile screenshots and judge journey.</action>
<verify><automated>npm --prefix frontend run test -- --run &amp;&amp; npm --prefix frontend run build</automated></verify>
<done>Tests and build pass; browser usability remains an explicit independent parent gate.</done>
</task>
</tasks>
<threat_model>Existing local report text is rendered as React text; no HTML injection, new endpoints, external data or trust boundaries. Keep gid strings lossless; cap simultaneous visible graph edges without dropping access to any neighbor.</threat_model>
<verification>All frontend tests, production build, and independent parent desktop/mobile browser journey using actual report.</verification>
<success_criteria>An analyst can choose a priority, read why, understand money direction, find any gid, inspect its limitations and export results without searching through stacked panels.</success_criteria>
<output>Create 03-01-SUMMARY.md. Parent owns serialized git commits, shared state/roadmap/requirements and independent browser verification.</output>
