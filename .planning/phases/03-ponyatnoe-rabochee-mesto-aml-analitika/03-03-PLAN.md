---
phase: 03
plan: 03
type: execute
autonomous: true
requirements: [UI-04, UX-01, UX-02, UX-03]
---
# Mercury-inspired analyst workspace

User-authorized full redesign, executed with Astra high. Parent serializes commits and shared planning state.

## Reference observed before implementation
On 2026-09-23, opened the real Mercury demo dashboard, Transactions and a Working Capital transaction detail in the browser. The demo rendered a dark theme. Borrow its approximately 210px persistent sidebar, modest sans headings, thin separators, compact transaction rows, right-aligned tabular amounts, subdued selected row and adjacent record detail. Adapt those structural patterns to a light AML workspace; do not copy banking actions, accounts, amounts or metrics.

## Tasks
1. Rebuild shared tokens and sidebar/workspace hierarchy; preserve data exploration.
2. Make selected directed edge evidence adjacent and keyboard accessible; integrate validated optional routes, resilience, anomalies and assistant.
3. Run regression and semantic tests, inspect desktop/narrow screenshots, record evidence and limits.

## Verification
Production build, unit tests, actual-data browser journey, screenshot review. No claim of transaction-row data when only aggregates exist.
