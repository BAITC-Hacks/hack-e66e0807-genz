# Roadmap: Граф денег

## Overview

GSD workflow disabled by explicit latest user instruction; this file is continuity only. Release deadline: 2026-09-23 12:36:30 UTC. Incomplete optional capabilities are excluded from the finished release.

PROJECT_MODE=mvp. Сначала аналитик получает полностью работоспособный локальный MVP, покрывающий все обязательные требования кейса и пригодный для сдачи. Общий контракт данных фиксируется до параллельной работы над аналитикой и интерфейсом shadcn/ui. После проверки MVP добавляются временные признаки и помощь в дальнейшей проверке гипотез. Фазы 1–2 реализованы; фаза 3 завершила техническую проверку редизайна и ожидает пользовательскую оценку читаемости. Последний запрос пользователя разрешает реализацию всех дополнительных направлений до рабочего результата; старый planning-only hold отменён. Аналитика, Mercury UI и ассистент выполняются параллельно по зафиксированному контракту; финальная интеграция проверяет исходный кейс.

## Phases

- [x] **Phase 1: Полный локальный MVP** — завершена 2026-09-23.
- [x] **Phase 2: Временные паттерны и углублённая проверка** — завершена 2026-09-23.
- [ ] **Phase 3: Понятное рабочее место AML-аналитика** — прежний редизайн проверен; новый Mercury-редизайн выполняет Astra.
- [ ] **Phase 4: Маршруты и возвратные потоки** — реализация начата.
- [ ] **Phase 5: Устойчивость сети при удалении узлов** — реализация начата.
- [ ] **Phase 6: Объяснимые аномалии относительно колена** — реализация начата.
- [ ] **Phase 7: AI-ассистент с проверяемыми ссылками** — реализация начата.

## Phase Details

### Phase 1: Полный локальный MVP

**Goal:** As a local AML analyst, I want to inspect explainable transaction-network priorities, so that I can reproduce and demonstrate all five required case capabilities.
Пользовательский результат: аналитик локально получает проверяемый ответ «кого смотреть первым и почему», находит любой gid и исследует связи; все пять must-have кейса готовы к сдаче.
**Mode:** mvp
**Depends on**: Nothing (first phase)
**Requirements**: DATA-01, DATA-02, DATA-03, ROLE-01, ROLE-02, ROLE-03, CLUS-01, RANK-01, UI-01, UI-02, UI-03, SHIP-01, SHIP-02, TEST-01
**Success Criteria** (what must be TRUE):

  1. Пользователь одной командой из README обрабатывает исходные edges/nodes/transactions parquet менее чем за 300 секунд и получает три CSV фиксированных схем и версионированный JSON для UI. Все входные gid, включая изоляты, представлены ровно один раз, gid в JSON строковые; проверка ссылок, сумм и числа транзакций выдаёт понятный результат.
  2. Для любого gid аналитик видит одну из шести ролей, role_score и priority_score в [0,1], cluster_id и непустое evidence длиной не более 200 символов; карточка и описание правил объясняют роль метриками, порогами и приоритетом пересекающихся правил. Depth=4 без исходящих не становится terminal только из-за обрыва; seed и неполные входящие явно отмечены, выводы сформулированы как гипотезы.
  3. Аналитик получает сообщество каждого узла и clusters.csv с cluster_id,n_nodes,n_seed,sum_kzt_internal,top_gids,hypothesis; top_nodes.csv содержит не менее 20 уникальных узлов с rank,gid,role,priority_score,why и объясняет порядок проверки.
  4. В локальном интерфейсе shadcn/ui аналитик видит направленные связи, роли, кластеры и легенду; поиск любого gid, включая изолят и граничный узел, открывает карточку и доступные связи, а выбор из топ-листа ведёт к тому же узлу. Три CSV доступны для скачивания; загрузка, ошибка, отсутствие результатов и ограничения данных понятны; интерфейс читаем на узком экране, доступен клавиатурой и после локальной сборки работает без CDN.
  5. Пользователь воспроизводит установку, расчёт и запуск UI по README, находит правила, ограничения и подход к масштабу около миллиона узлов, использует диаграмму решения и сценарий пятиминутного демо с живым прогоном и разбором 2–3 узлов. Проверки ролей, обрыва, seed, изолятов, CSV/JSON, воспроизводимости и основного пути UI проходят; время полного прогона реальных данных зафиксировано.

**Plans**: 3 plans

Plans:

- [x] 01-01-PLAN.md — Реальные данные, объяснимые роли, сообщества и детерминированные выгрузки (wave 1).
- [x] 01-02-PLAN.md — Параллельный React/Vite shadcn UI: поиск, направленный граф, карточки и скачивание (wave 1).
- [x] 01-03-PLAN.md — Локальный запуск, интеграционные проверки, README, диаграмма и демо (wave 2).

**UI hint**: yes

**Execution constraints**: Сначала документированный JSON/CSV-контракт и согласованный пример данных, затем параллельно аналитика и shadcn UI с раздельным владением файлами; после этого интеграция, измеренный реальный прогон, проверка критериев и устранение пробелов. Данные организаторов не менять; результаты и gid не хардкодить. Часовой лимит MVP отсчитывается от 2026-09-23 10:47 UTC. Детальное разбиение планов выполняется в plan-phase.

### Phase 2: Временные паттерны и углублённая проверка

**Goal**: Аналитик уточняет гипотезы по временным паттернам, исследует сообщества через фильтры и видит, какие недостающие данные запросить дальше.
**Depends on**: Phase 1
**Requirements**: TIME-01, EXPL-01
**Success Criteria** (what must be TRUE):

  1. Аналитик видит вычисленные по transactions признаки поступления и последующих переводов в пределах 1–2 дней, всплесков активности и синхронных поступлений от нескольких плательщиков; объяснение указывает наблюдаемые даты и метрики и предупреждает, что совпадение во времени не доказывает идентичность денег.
  2. В карточке узла аналитик получает следующий запрос недостающих данных, связанный с обнаруженным ограничением выборки или проверяемой гипотезой.
  3. Аналитик с помощью дополнительных фильтров и обзора кластеров сужает сеть до интересующих сообществ и узлов и открывает их объяснимые карточки.

**Plans**: 3 plans

- [x] 02-01-PLAN.md — Временная аналитика, профиль входящих и следующие запросы данных (wave1).
- [x] 02-02-PLAN.md — Параллельный shadcn UI: временная карточка, Select-фильтры и обзор кластеров (wave1).
- [x] 02-03-PLAN.md — Независимая проверка реальных расчётов, браузерный сценарий и документация (wave2).

**UI hint**: yes

**Execution constraints**: Начинать после подтверждения всех критериев Phase 1; сохранить схемы обязательных CSV и совместимость JSON-контракта либо явно версионировать его изменение. TIME-01 и EXPL-01 — улучшения v2; AI-01 отложен и не блокирует завершение.

## Progress

**Execution Order:** Phase 1 → 2 → 3 → 4 → 5 → 6 → 7. Planning-only hold отменён последующим запросом завершить рабочий продукт. Независимые модули выполняются параллельно; итоговая проверка обязательна.

| Phase | Plans Complete | Status | Completed |
|-------|----------------|--------|-----------|
| 1. Полный локальный MVP | 3/3 | Complete | 2026-09-23 |
| 2. Временные паттерны | 3/3 | Complete | 2026-09-23 |
| 3. Рабочее место аналитика | 3/3 | Complete: Mercury UI verified | 2026-09-23 |
| 4. Маршруты и возвраты | 2/2 | Complete | 2026-09-23 |
| 5. Устойчивость | 2/2 | Complete | 2026-09-23 |
| 6. Аномалии | 2/2 | Complete | 2026-09-23 |
| 7. AI-ассистент | Integration only | Optional: live provider gate pending | - |

## Coverage

33 требования связаны с фазами в REQUIREMENTS.md. Обрыв графа, временные признаки, карточка и следующие запросы данных реализованы. Маршруты, устойчивость и аномалии реализованы и независимо проверены. AI-адаптер реализован, но реальный ответ провайдера пока не подтверждён; функция не заявляется завершённой. UI-04 перенесён из исходной приёмки в фазу 3; технические проверки не подменяют пользовательскую оценку читаемости.

### Phase 3: Понятное рабочее место AML-аналитика

**Goal:** Аналитик понимает кого проверять первым и почему, читает направление и суммы переводов и открывает доказательные признаки без перегруженного экрана.
**Requirements**: UI-04, UX-01, UX-02, UX-03
**Success Criteria:**

1. Первый экран показывает приоритеты, крупную схему и компактную карточку выбранного счёта.
2. На графе понятны отправитель, получатель, сумма и роль; плотные окрестности не скрываются без объяснения.
3. Все исходные функции проходят повторный сценарий, а читаемость проверена по реальным desktop/mobile снимкам.

**Depends on:** Phase 2
**Plans:** 2 plans

Plans:

- [x] 03-01-PLAN.md — Astra: дизайн-контракт и новый интерфейс, граф и компонентные проверки.
- [x] 03-02-PLAN.md — Независимая приёмка, регрессия кейса, документация и доставка.

User acceptance correction: Phase 1/2 functional checks passed, but UI readability was rejected. Phase 3 is required before claiming the interface complete.

### Phase 4: Маршруты и возвратные потоки

**Goal:** Аналитик находит ограниченные, воспроизводимые направленные маршруты и возвраты, видит их суммы и наблюдаемый порядок дат без ложного вывода об идентичности денег.
**Requirements**: ROUTE-01, ROUTE-02, ROUTE-03
**Depends on:** Phase 3
**Success Criteria:**

1. Для выбранного gid видны рассчитанные A→B→C маршруты и возвратные циклы из 2–3 участников со ссылками на узлы и суммами по каждому ребру.
2. Повторение определяется числом различных дней начала наблюдаемой последовательности; одинаковые даты отмечены как неоднозначные, пределы перебора и неполнота результатов видимы.
3. Перестановка входных строк не меняет результат; ограниченный расчёт, тесты отрицательных примеров и браузерная навигация проходят, исходные три CSV остаются неизменными.

**Execution authorization:** Пользователь разрешил завершить реализацию и исправлять до прохождения критериев; без будущих обещаний или незавершённых функций.

**Plans:** 2 plans

Plans:
**Wave 1**

- [x] 04-01-PLAN.md — Сквозной маршрут и возврат с ограниченным детерминированным расчётом и карточкой (wave 1).

**Wave 2** *(blocked on Wave 1 completion)*

- [x] 04-02-PLAN.md — Дата-свидетельства, циклы, ограничения и независимая приёмка (wave 2).

### Phase 5: Устойчивость сети при удалении узлов

**Goal:** Аналитик сравнивает наблюдаемую связность сети до и после удаления первых N узлов существующего приоритета и видит корректные знаменатели и ограничения сценария.
**Requirements**: RES-01, RES-02, RES-03
**Depends on:** Phase 4
**Success Criteria:**

1. Сценарии N=0..min(20, размер топа) удаляют один и тот же детерминированный префикс исходного приоритета из копии графа.
2. Для каждого сценария показаны число компонент, размер крупнейшей, изоляты и доли с явно указанными знаменателями; пустой граф обработан без деления на ноль.
3. Независимые примеры подтверждают результаты, UI позволяет сравнивать до/после, исходный граф и CSV не меняются, полный батч укладывается в 300 секунд.

**Execution authorization:** Пользователь разрешил завершить реализацию и исправлять до прохождения критериев; без будущих обещаний или незавершённых функций.

**Plans:** 2 plans

Plans:
**Wave 1**

- [x] 05-01-PLAN.md — Сквозной сценарий N=1 и серия фиксированных top-N удалений (wave 1).

**Wave 2** *(blocked on Wave 1 completion)*

- [x] 05-02-PLAN.md — Граничные случаи, сравнение в UI и независимая приёмка (wave 2).

### Phase 6: Объяснимые аномалии относительно колена

**Goal:** Аналитик получает воспроизводимые кандидаты на проверку, сравниваемые с тем же коленом, с исходной метрикой, базой сравнения и оговорками цензурирования.
**Requirements**: ANOM-01, ANOM-02, ANOM-03
**Depends on:** Phase 5
**Success Criteria:**

1. Каждая показанная аномалия имеет исходную метрику, размер сопоставимой группы того же колена, базовое значение и конкретное правило с порогом.
2. Малые группы, нулевой MAD, неполные входы seed и выходы четвёртого колена дают предусмотренный контрактом результат без ложной точности; невидимые переводы ниже порога не достраиваются.
3. Положительные и отрицательные примеры, старый JSON без расширения, UI-объяснения и независимая регрессия проходят; роли, приоритеты и исходные CSV сохраняются.

**Execution authorization:** Пользователь разрешил завершить реализацию и исправлять до прохождения критериев; без будущих обещаний или незавершённых функций.

**Plans:** 2 plans

Plans:
**Wave 1**

- [x] 06-01-PLAN.md — Сквозной профиль относительно колена и устойчивые базовые оценки (wave 1).

**Wave 2** *(blocked on Wave 1 completion)*

- [x] 06-02-PLAN.md — Наблюдаемые суммы у порога, цензурирование и независимая приёмка (wave 2).

### Phase 7: AI-ассистент с проверяемыми ссылками

**Goal:** При явно подключённом бесплатном внешнем провайдере аналитик задаёт вопрос о снимке графа и получает ответ со ссылками на существующие gid и доказательства; отсутствие модели не мешает базовой работе.
**Requirements**: AI-01, AI-02, AI-03, AI-04
**Depends on:** Phase 6
**Success Criteria:**

1. При включённом реальном LLM вопрос на естественном языке вызывает ограниченные инструменты и возвращает проверяемые факты со ссылками; живой обмен с бесплатным провайдером подтверждён отдельно от mock-тестов.
2. Сервер проверяет не только существование gid, но и значения структурированных утверждений; ложная сумма, неверная ссылка, внедрённая инструкция и отсутствующие данные обрабатываются определённым безопасным ответом.
3. Без модели базовый локальный продукт и три CSV работают; оценочные сценарии, таймауты, отключённый режим, границы данных и все пять must-have проходят регрессию. Отсутствие реальной модели не выдаётся за завершённую AI-функцию.

**Execution authorization:** Пользователь разрешил завершить реализацию и исправлять до прохождения критериев; без будущих обещаний или незавершённых функций.

**Plans:** 3 plans

Plans:
**Wave 1**

- [ ] 07-01-PLAN.md — Сквозной реальный LLM-вопрос через ограниченный read-only инструмент (wave 1).

**Wave 2** *(blocked on Wave 1 completion)*

- [ ] 07-02-PLAN.md — Набор запросов, проверка цитат и защита границ доверия (wave 2).

**Wave 3** *(blocked on Wave 2 completion)*

- [ ] 07-03-PLAN.md — UI, оценка качества и регрессия базового продукта (wave 3).
